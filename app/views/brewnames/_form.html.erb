<%= form_for(@brewname) do |f| %>
  <% if @brewname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brewname.errors.count, "error") %> prohibited this brewname from being saved:</h2>

      <ul>
      <% @brewname.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
