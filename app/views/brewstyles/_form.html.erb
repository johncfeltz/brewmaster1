<%= form_for(@brewstyle) do |f| %>
  <% if @brewstyle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brewstyle.errors.count, "error") %> prohibited this brewstyle from being saved:</h2>

      <ul>
      <% @brewstyle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :style %><br />
    <%= f.text_field :style %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
